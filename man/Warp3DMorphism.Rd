% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morphism.R
\name{Warp3DMorphism}
\alias{Warp3DMorphism}
\title{Create a 3D warp field morphism}
\usage{
Warp3DMorphism(
  source,
  target,
  warp_path,
  warp_type = c("ants", "ants_h5", "fsl", "fsl_coef", "afni", "freesurfer", "dense"),
  inverse_path = "",
  def_type = NULL,
  warp_method = c("linear", "cubic"),
  cost = 1.5,
  method_tag = "anatomical",
  source_affine = NULL,
  source_dim = NULL,
  target_affine = NULL,
  target_dim = NULL
)
}
\arguments{
\item{source}{Source domain hash}

\item{target}{Target domain hash}

\item{warp_path}{Path to displacement field file}

\item{warp_type}{One of "ants", "ants_h5", "fsl", "fsl_coef", "afni", "freesurfer", "dense"}

\item{inverse_path}{Path to inverse warp (optional)}

\item{def_type}{Deformation type: "relative" (displacement) or "absolute" (coordinates)}

\item{warp_method}{Interpolation method for warp field lookup: "linear" or "cubic"}

\item{cost}{Path cost (default 1.5)}

\item{method_tag}{Method tag (default "anatomical")}

\item{source_affine, source_dim}{Source image voxel-to-RAS affine and three
dimensions. Required for FSL warps (\code{warp_type = "fsl"} or
\code{"fsl_coef"}), and construction fails without them: FSL field values
are source scaled-voxel coordinates, which cannot be recovered from the warp.}

\item{target_affine, target_dim}{Reference image voxel-to-RAS affine and three
dimensions. For dense FSL fields they default to the warp lattice; supply
both when the field lattice differs from the reference image grid. Required
for FNIRT coefficient files (\code{"fsl_coef"}), which store the reference
dimensions and voxel size but not its orientation or origin.}
}
\value{
Warp3DMorphism object
}
\description{
Represents a nonlinear warp field transformation.
}
\examples{
warp <- Warp3DMorphism("native", "mni", "path/to/warp.nii.gz")
}
