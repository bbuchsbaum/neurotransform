% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampler.R
\name{backproject_surface_to_volume}
\alias{backproject_surface_to_volume}
\title{Backproject surface values into a volume (adjoint of VolToSurf sampling)}
\usage{
backproject_surface_to_volume(
  surface_values,
  morphism,
  grid,
  surface_coords = NULL,
  method = c("linear", "nearest", "ribbon"),
  outside = 0
)
}
\arguments{
\item{surface_values}{Numeric vector (length N) or matrix (N x k) of values at surface points}

\item{morphism}{VolToSurfMorphism}

\item{grid}{Source Grid (volume geometry)}

\item{surface_coords}{Optional N x 3 world coordinates; defaults to morphism@params$mid_coords}

\item{method}{"linear", "nearest", or "ribbon"}

\item{outside}{Value used for out-of-bounds (default 0)}
}
\value{
3D array (or 4D if multivariate surface_values) in grid geometry
}
\description{
Implements a simple backprojection operator that distributes each surface
value into the source volume grid using the same interpolation weights that
forward sampling would use.
}
\details{
This is the transpose/adjoint of the linear sampling operator for
\code{sample_volume_on_surface(..., method="linear")} when the same
\code{surface_coords} are used.
}
