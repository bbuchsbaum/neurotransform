% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlap_metrics.R
\name{overlap_metrics}
\alias{overlap_metrics}
\title{Image overlap metrics}
\usage{
overlap_metrics(
  x,
  y,
  threshold = 0,
  labels = NULL,
  na.rm = TRUE,
  empty = c("one", "zero", "na"),
  check_geometry = TRUE,
  tolerance = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{x, y}{Arrays or image objects with matching dimensions.}

\item{threshold}{Numeric scalar used to form foreground masks when \code{labels}
is \code{NULL}.}

\item{labels}{Optional vector of label values to compare. When supplied,
\code{threshold} is ignored and one row is returned per label.}

\item{na.rm}{Logical; if \code{TRUE}, paired missing or non-finite numeric voxels
are removed before computing masks.}

\item{empty}{How to score Dice, Jaccard, overlap coefficient, and volume
similarity when both masks are empty. \code{"one"} treats two empty masks as
perfect agreement, \code{"zero"} returns 0, and \code{"na"} returns \code{NA_real_}.}

\item{check_geometry}{Logical; if \code{TRUE}, compare available voxel-to-world
affines after confirming that dimensions match.}

\item{tolerance}{Numeric tolerance used for affine comparisons.}
}
\value{
A data frame with voxel counts and overlap metrics. \code{sensitivity}
is \code{intersection / n_x}; \code{precision} is \code{intersection / n_y}.
}
\description{
Compute voxelwise overlap summaries for two images or masks. Inputs may be
plain arrays or image objects that can be coerced with \code{as.array()}, including
\code{neuroim2} volumes.
}
\details{
By default, numeric images are converted to foreground masks with
\code{value > threshold}, and logical images use \code{TRUE} as foreground. Supply
\code{labels} to compute label-wise overlap instead.
}
\examples{
x <- array(c(1, 1, 0, 0), dim = c(2, 2, 1))
y <- array(c(1, 0, 1, 0), dim = c(2, 2, 1))
overlap_metrics(x, y)
}
